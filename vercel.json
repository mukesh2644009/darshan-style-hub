{
  "buildCommand": "prisma generate && prisma db push && next build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/abandoned-cart",
      "schedule": "0 6 * * *"
    }
  ]
}

